const fs = require("fs"); // Importamos el modulo fs para poder trabajar con archivos

// importamos la funcion para incrementar los alumnos
const { incrementarAlumnos } = require('./adminController');

exports.cargarAlumnons = (req, res) => {

};